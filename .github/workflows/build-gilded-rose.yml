name: build-gilded-rose
on:
  push:
    branches:
    - '**'
    paths:
      - gilded-rose/**
      - .github/workflows/build-gilded-rose.yml

jobs:
  build-project:
    name: build-gilded-rose
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'maven'
      - run: cd gilded-rose && mvn clean compile org.pitest:pitest-maven:mutationCoverage verify package -DnvdApiKey=${{ secrets.NVD_API_KEY }}

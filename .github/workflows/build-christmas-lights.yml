name: build-christmas-lights
on:
  push:
    branches:
    - '**'
    paths:
      - christmas-lights/**
      - .github/workflows/build-christmas-lights.yml

jobs:
  build-project:
    name: build-christmas-lights
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'maven'
      - run: cd christmas-lights && mvn clean compile org.pitest:pitest-maven:mutationCoverage verify package -DnvdApiKey=${{ secrets.NVD_API_KEY }}
